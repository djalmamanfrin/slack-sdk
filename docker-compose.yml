version: '3'

services:
  php:
    container_name: php-slack-sdk
    build:
      context: ./dockerfiles/php
    volumes:
      - .:/var/www
    environment:
      PHP_IDE_CONFIG: "serverName=slack-sdk"

  nginx:
    container_name: nginx-slack-sdk
    restart: always
    build:
      context: ./dockerfiles/nginx
    ports:
      - "80:80"
    volumes:
      - .:/var/www
    depends_on:
      - php